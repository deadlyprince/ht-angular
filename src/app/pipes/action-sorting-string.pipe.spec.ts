import { ActionSortingStringPipe } from './action-sorting-string.pipe';

describe('ActionSortingStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionSortingStringPipe();
    expect(pipe).toBeTruthy();
  });
});
