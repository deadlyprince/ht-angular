import { ActionStatusStringPipe } from './action-status-string.pipe';

describe('ActionStatusStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionStatusStringPipe();
    expect(pipe).toBeTruthy();
  });
});
