import { DistanceLocalePipe } from './distance-locale.pipe';

describe('DistanceLocalePipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceLocalePipe();
    expect(pipe).toBeTruthy();
  });
});
