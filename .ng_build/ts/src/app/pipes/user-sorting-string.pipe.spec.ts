import { UserSortingStringPipe } from './user-sorting-string.pipe';

describe('UserSortingStringPipe', () => {
  it('create an instance', () => {
    const pipe = new UserSortingStringPipe();
    expect(pipe).toBeTruthy();
  });
});
